
전처리
<thread> 
<mutex> 

== 
호출 함수



실행문에서 
	std::thread 쓰레드 객체1(호출 함수);
	std::thread 쓰레드 객체2(호출 함수);

	쓰레드 객체1.join() <= 기다린다.
	쓰레드 객체2.join() <= 기다린다.

	Lock.lock() 내가 실행할때까지 기다려
	Lock.unlock() 다쓰면 풀게
	락은 최소한으로 써야한다.

	 - 인터락