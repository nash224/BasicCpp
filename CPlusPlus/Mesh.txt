디포머
	- 애니메이션 정보가 들어있는 3D 매쉬


스키닝


Bone == float4x4 ArrBone[100] == 행렬
{
	- 본은 부모자식 구조를 가짐
	- 일반적으로 엉덩이에서 시작
	- 메쉬로 감싸져있음
		- 본은 몇번째 본으로 영향을 받는다는 인덱스가 있어야한다.
		- 관절 부분은 본의 N개 영향을 받을 수 있다.
			- N개 영향을 받을 때, 비율로 받는다.
			- BLENDINDICES = 몇번째 본에 영향을 받을꺼냐
			- WEIGHT = 비중은 얼마냐
				=> IW

	- Defuse Texture 
	{
		- 피부의 텍스처
	}
	- Normal Texture 
	- SubSet
	{
		- 정점은 하나지만 연결하는 방식은 n개
	}

	- 컴파일 타임 어설션
	{
		- 어떻게 컴파일시간에 처리되냐.
		- 최적화 요구
		- 컴파일 타임에 결정이 되어 있다.
		- 오버로딩 우선순위 => 상속 순위
	}

	- 노말과 UV는 메쉬와 다를수 있어서 따로 분리해서 얻어와야한다.
	- fbx를 어떤 Mat으로 지정(GameEngineFrameWork)
	- fbxMesh >> Vertex >> Index >> GameEnigneMesh

	- fbx Mesh
	- 현대 Mesh 데이터는 하나의 버텍스 뭉텅이로 이루어져 있다.
	- 팔 인덱스 Subset

	- DirectX 아핀 행렬
	- SCale Origin
	- Tolerance : 극치 => 아주 작은값이면 0으로 맞춰줌

	- 클러스터 인덱스에 값이 없으면 관절이 없고 => 본도 없다
	- 스캘래톤 매쉬 노드

	- 다수 프레임들을 줄여 일부 Pose만 남긴다.
	- 클러스터에 연결되어 있는 노드들이 본이다.
	- 본 매쉬에서 클러스터는 중복으로 들어갈 수 없다.
	
	- 애니메이션을 하려면 본 개수만큼 보간된 행렬을 쉐이더에 넣어줘야한다.

} 


