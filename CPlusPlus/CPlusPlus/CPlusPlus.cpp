// CPlusPlus.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include <iostream>
#include <string>

int main()
{
	int Number = 352'232'235;

	////////////////////////////////////////////////////////////////////////////////////////////////////////////
	  //////////////////////////////////////// Test3. Escape String Test ////////////////////////////////////////////////
	////////////////////////////////////////////////////////////////////////////////////////////////////////////
	{
		std::string str1 = "Hello \" WOrld,";
		std::cout << str1 << std::endl;
		                                                                                                       
		std::string str2 = R"(Hello, " ", World)";
		std::cout << str2 << std::endl;
	}

	return 0;
	////////////////////////////////////////////////////////////////////////////////////////////////////////////
	  //////////////////////////////////////// Test3. stoi & to_string Function ////////////////////////////////////////////////
	////////////////////////////////////////////////////////////////////////////////////////////////////////////

	{
		// Number To string

		int Number1 = 66666;
		std::string ConvertNubmer1 = std::to_string(Number1);
		std::cout << ConvertNubmer1 << std::endl;

		const std::string StrNumber1 = "1911555653"; // << 나타낼수 있는 수만큼 더 큰 문자열 숫자라면 out_of_range exception을 일으킨다.
		int iNum = stoi(StrNumber1);
		std::cout << iNum << std::endl;


		// string To Number
	}


	return 0;

	{
		long double LDouble = 14.6L; // << 8byte
		std::cout << sizeof(LDouble) << std::endl;
	}

	return 0;

	////////////////////////////////////////////////////////////////////////////////////////////////////////////
	  //////////////////////////////////////// Test2. strcmp Function ////////////////////////////////////////////////
	////////////////////////////////////////////////////////////////////////////////////////////////////////////

	{
		const char* str1 = "MyName";
		char str2[] = "MyName";

		std::cout << "C 스타일 Character Pointer 비교 : " << (strcmp(str1, str2) == 0) << std::endl;
	}

	return 0;

	////////////////////////////////////////////////////////////////////////////////////////////////////////////
	  //////////////////////////////////////// Test1. literal Pooling ////////////////////////////////////////////////
	////////////////////////////////////////////////////////////////////////////////////////////////////////////


	//  VS 는 문자열 리터럴을 char* 에 대입할수 없음 
	// 이유? 컴파일러가 메모리를 최적화하기위해 읽기 전용 메모리로 리터럴 풀링을 적용
	// char* str = "Hello"; 

	return 0;

	////////////////////////////////////////////////////////////////////////////////////////////////////////////
	  //////////////////////////////////////// Test0. strlen ////////////////////////////////////////////////
	////////////////////////////////////////////////////////////////////////////////////////////////////////////

	{
		const char* str1 = "Hello";
		char* result = new char[strlen(str1) + 1];
		int StrSize = static_cast<int>(strlen(str1) + 1);
		strcpy_s(result, StrSize, str1);

		std::cout << StrSize << std::endl;
		std::cout << result << std::endl;
		std::cout << "Str Pointer Size : " << sizeof(str1) << std::endl;
	}

	return 0;

}
